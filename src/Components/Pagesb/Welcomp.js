/*// Welcomp.js

import React from 'react';
import { useNavigate } from 'react-router-dom';

function Welcomp() {
  const navigate = useNavigate();

  const goToDashboard = () => {
    navigate('/Dashboard');
  };

  return (
    <div>
      <button onClick={goToDashboard}>GO</button>
    </div>
  );
}

export default Welcomp;*/
